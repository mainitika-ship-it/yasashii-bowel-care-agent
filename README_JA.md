# やさしい排便モニター — 実行ガイド

介護者の「観察・記録・申し送り」を助ける試作です。診断機器ではありません。
正式な英語の説明は [README](README.md) を参照してください。

**9月11日：MacのStrands＋qwen3:8bによる3ケース成功ログを受領・照合済みです。** 観察記録・確認待ち・安全停止は各1件、申し送りは観察1件だけです。[実行時のコードと一致する検証記録](docs/verification_2026-09-11.md)を保存しました。

## まず無料で動かす

このリポジトリをダウンロードし、READMEが入ったフォルダで実行します。Python 3.10以上が必要です。
MacやLinuxで `python` が見つからなければ、`python3` に置き換えてください。

```bash
python src/demo.py --mode offline
```

AWSへの接続・認証情報・追加ライブラリのインストールは不要です。安全な模擬データだけで、次の一連動作を試します。

| 表示 | 意味 | 残すもの |
|---|---|---|
| PASS | 観察を記録 | 観察記録1件 |
| HOLD | 人の確認待ち | 確認待ち1件。事実として確定しない |
| STOP | 安全のため停止 | 安全停止1件。観察記録には加えない |

実行後に表示される `view_path` の `report.html` をブラウザで開くと、結果が日英併記で見られます。
申し送りの観察件数は **1件** です。確認待ちと安全停止を足して3件にはしません。
毎回別の実行フォルダを作るため、前回の結果と混ざりません。

**「無料の模擬実行」は実際のAIモデルが動いた証拠ではありません。** 画面にも実行モードを明記します。
画面は閲覧専用で、介護者の承認ボタンはまだありません。本人やご家族の画像・実際の介護記録は使わないでください。

## 私たちが優先する順番

1. 無料の3ケースを再現し、安全停止と二重記録防止を確認する。
2. この専用公開リポジトリのREADME・MITライセンス・自動テストを確認する。
3. Strands＋ツール呼出し対応モデルで3ケースを実行し、成功結果を保存する。ローカルOllamaか、費用を了承したBedrockを選べる。
4. 成功した実行画面を使い、英語または英語字幕付きの5分以内の公開動画を作る。
5. Devpostの古いリポジトリURLを専用リポジトリに差し替え、[構成図PNG](docs/architecture.png)を添付する。必須欄と公開リンクを本人が確認してから最終Submitする。

公式締切は **2026年9月15日9:00（日本時間）**。[公式要件の再検証記録](docs/hackathon_recheck.md)に確認内容をまとめています。

## 次の最小作業

成功ログの照合は済みました。次は、保存した結果画面と動作を説明する**5分以内の動画**を作ります。実行方式と模擬データの表示を残し、英語または英語字幕を付けます。

## 別のMac・新しい環境で準備する場合

主催者の要件はStrandsの使用で、Bedrockや特定モデルの使用は必須ではありません。
最新版をMacにダウンロードし、このREADMEのあるフォルダで次の1行を実行します。モデル名の入力は不要です。

```bash
python3 src/check_local_setup.py
```

既存モデルとPythonライブラリを調べ、準備が整っていれば、そのMacで使える実行コマンドを表示します。複数あれば1つを選びます。確認ツール自体はダウンロードやAI推論を行いません。
「確認必要」と出たら、まだ実行条件を確認できていない状態です。表示結果を共有してください。モデルを買い足す必要はまだありません。
詳しい手順は[ローカル実行ガイド](docs/local_model_guide.md)にあります。画像を読めるモデルでもツール呼出しに対応するとは限りません。

実機カメラ連携・常時監視・介護者の承認画面は、この提出用デモとは別に検証が必要です。
AWSの実行方法は英語READMEの手順に従います。`--allow-paid-model` は費用を伴う実行の許可です。
Macでの実モデル実行は上記の保存ログで確認しました。この取り込み作業ではAI推論を再実行していません。AWSの有料実行・最終Submitは未実施です。
